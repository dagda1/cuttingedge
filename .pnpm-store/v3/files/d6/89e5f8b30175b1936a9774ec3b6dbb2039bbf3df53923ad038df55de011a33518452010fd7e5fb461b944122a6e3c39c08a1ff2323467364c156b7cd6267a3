import { createFuture } from '../future';
export function createFutureController(task, future) {
    let { future: inner, produce } = createFuture();
    function start() {
        future.consume(produce);
    }
    function halt() {
        produce({ state: 'halted' });
    }
    return { start, halt, future: inner, type: 'future', operation: future };
}
//# sourceMappingURL=future-controller.js.map