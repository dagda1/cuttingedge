export declare class ColorModel {
    private userColors;
    private normalizeColor(model, def);
    getColor(model: string, def: string): string;
    private getColorByName(name);
    defineColor(model: string, name: string, def: string): void;
}
