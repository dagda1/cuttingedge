import { FullPageLayout } from '../components/Layouts/FullPageLayout';
import { Retainer } from '../components/Retainer/Retainer';

function RetainerService(): JSX.Element {
  return (
    <FullPageLayout>
      <Retainer />
    </FullPageLayout>
  );
}

export default RetainerService;
