import '../config/env';
export declare const build: ({
  buildClient,
  buildServer,
  buildNode,
}: {
  buildClient: boolean;
  buildServer: boolean;
  buildNode: boolean;
}) => Promise<void>;
//# sourceMappingURL=build.d.ts.map
